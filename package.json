{
  "name": "nodejs-logger-lktyim",
  "version": "1.0.0",
  "description": "Develop the command line node.js application, which parses the input log file (see Input Example). The application should find all the log messages with the level error and print them into the output file (see Output Format).",
  "main": "index.js",
  "scripts": {
    "test": "jest",
    "test:coverage": "jest --runInBand --coverage",
    "start": "node build/parser.js",
    "start:dev": "node -r ts-node/register src/parser.ts",
    "build": "tsc"
  },
  "repository": {
    "type": "git",
    "url": "http://berbob-gmbh-abtvtm@git.codesubmit.io/berbob-gmbh/nodejs-logger-lktyim"
  },
  "author": "aregsyan",
  "license": "ISC",
  "dependencies": {
    "inversify": "^6.0.1",
    "reflect-metadata": "^0.1.13",
    "yargs": "^17.7.2"
  },
  "devDependencies": {
    "@types/jest": "^29.5.3",
    "@types/yargs": "^17.0.24",
    "jest": "^29.6.2",
    "ts-jest": "^29.1.1",
    "ts-node": "^10.9.1",
    "typescript": "^5.1.6"
  }
}
